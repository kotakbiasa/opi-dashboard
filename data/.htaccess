# Block all web access to sensitive JSON data (auth hashes, vouchers, members, sessions)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
